{% import "_includes/forms" as forms %}

{% set fieldId = fieldId ?? 'remix' %}

{{ forms.text({
    id: fieldId,
    name: fieldId,
    readonly: true,
    value: value,
    class: 'remix-field',
    disabled: true,
    instructions: "This field updates automatically when you save the entry."|t('remix'),
}) }}
